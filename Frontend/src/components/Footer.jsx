export default function Footer() {
  return (
    <footer className="footer">
      <div className="footer__inner">
        <span>ShelfStock — a storefront wired straight to your Django API.</span>
        <span className="footer__meta">Stock is live. Every price tag below came off the backend, not a mock file.</span>
      </div>
    </footer>
  )
}
